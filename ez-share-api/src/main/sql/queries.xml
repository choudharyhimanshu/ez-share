<queries>
    <query name="createFileTable">
        CREATE TABLE shared_files(
        id uuid PRIMARY KEY,
        name VARCHAR (100) NOT NULL,
        created_on TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        size BIGINT NOT NULL,
        directory VARCHAR (60) NOT NULL
        );
    </query>
</queries>
